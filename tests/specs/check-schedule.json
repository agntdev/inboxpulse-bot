[
  {
    "name": "Schedule Check",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Create recurring check job (default 1/hour if no interval specified)"
            }
          }
        ],
        "send": {
          "callback": "check:schedule"
        }
      }
    ]
  }
]
