[
  {
    "name": "Link Account",
    "steps": [
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Microsoft connection isn’t set up yet. Ask the owner to configure the Microsoft OAuth application, then try again."
            }
          }
        ],
        "send": {
          "callback": "auth:link"
        }
      }
    ]
  }
]
